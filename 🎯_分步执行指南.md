# 🎯 PostgreSQL脚本 - 分步执行指南

> **更稳妥的执行方式：先创建表，再插入数据**

---

## 📦 文件说明

### 已准备好的文件

| 文件 | 大小 | 内容 | 用途 |
|------|------|------|------|
| **mall_base_ddl.sql** ⭐ | 38 KB | 49个CREATE TABLE | 第1步：创建表结构 |
| **mall_base_dml.sql** ⭐ | 792 KB | 56个INSERT INTO | 第2步：插入数据 |
| **mall_base_final.sql** | 850 KB | DDL + DML完整 | 一次性执行 |

---

## 🚀 推荐执行方式（分两步）

### 方法A: 在DataGrip中执行（推荐）

#### Step 1: 创建表结构

1. 打开文件: `sql/postgresql/mall_base_ddl.sql`
2. 点击 ▶️ 执行
3. 等待完成（~5秒）
4. **验证**: 应该创建49个表

#### Step 2: 插入数据

1. 打开文件: `sql/postgresql/mall_base_dml.sql`
2. 点击 ▶️ 执行  
3. 等待完成（~1分钟）
4. **验证**: 所有表都有数据

### 方法B: 命令行执行

```bash
cd "/Users/admin/ma jia/ruoyi-vue-pro"

# Step 1: 创建表（49个表）
psql -U your_username -d ruoyi-vue-pro \
  -f sql/postgresql/mall_base_ddl.sql

# 验证表是否创建成功
psql -d ruoyi-vue-pro -c "\dt"

# Step 2: 插入数据
psql -U your_username -d ruoyi-vue-pro \
  -f sql/postgresql/mall_base_dml.sql

# 验证数据
psql -d ruoyi-vue-pro -c "SELECT COUNT(*) FROM product_brand;"
```

---

## ✅ 执行后验证

### 验证脚本

```sql
-- 1. 检查表数量
SELECT COUNT(*) as total_tables 
FROM information_schema.tables 
WHERE table_schema = 'public';
-- 预期: 49

-- 2. 检查关键表
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name LIKE 'product%'
ORDER BY table_name;
-- 应该显示所有product_*表

-- 3. 检查数据
SELECT 
  (SELECT COUNT(*) FROM product_brand) as brands,
  (SELECT COUNT(*) FROM product_category) as categories,
  (SELECT COUNT(*) FROM product_spu) as spus,
  (SELECT COUNT(*) FROM product_sku) as skus;
-- 应该都有数据

-- 4. 测试JSON字段
SELECT id, name, properties::jsonb 
FROM product_sku 
WHERE id = 1;
-- JSON应该正常显示
```

---

## 🎯 完整部署流程

### 三个阶段

```bash
#!/bin/bash
cd "/Users/admin/ma jia/ruoyi-vue-pro"

echo "========================================="
echo "康复训练器材共享平台 - 完整部署"
echo "========================================="

# 阶段1: 商城基础表
echo -e "\n阶段1: 创建商城基础表..."
psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_ddl.sql
if [ $? -eq 0 ]; then
  echo "✓ 表结构创建成功"
  
  echo -e "\n阶段1.5: 插入基础数据..."
  psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_dml.sql
  if [ $? -eq 0 ]; then
    echo "✓ 数据插入成功"
  fi
fi

# 阶段2: 器材共享改造
echo -e "\n阶段2: 应用器材共享改造..."
psql -d ruoyi-vue-pro -f sql/postgresql/equipment_renovation.sql
if [ $? -eq 0 ]; then
  echo "✓ 器材共享改造完成"
fi

# 阶段3: 交接会话功能
echo -e "\n阶段3: 部署交接会话功能..."
psql -d ruoyi-vue-pro -f sql/postgresql/equipment_handover.sql
if [ $? -eq 0 ]; then
  echo "✓ 交接会话功能部署成功"
fi

# 最终验证
echo -e "\n最终验证..."
TABLE_COUNT=$(psql -d ruoyi-vue-pro -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public'" | tr -d ' ')
echo "✓ 总表数量: $TABLE_COUNT"

echo "========================================="
echo "🎉 部署完成！"
echo "========================================="
```

---

## 💡 为什么分步执行更好？

### 优势

1. **更容易排查问题**
   - 如果DDL失败，立即知道是表结构问题
   - 如果DML失败，知道是数据问题

2. **可以跳过数据插入**
   - 如果只需要表结构，执行DDL即可
   - 如果已有数据，可以跳过DML

3. **更清晰的进度**
   - 第一步：49个表创建完成 ✅
   - 第二步：数据插入完成 ✅

4. **更安全**
   - 表创建失败不会影响后续
   - 可以分批执行和回滚

---

## 📁 文件清单

```
sql/postgresql/
├── mall_base_final.sql         完整SQL (DDL+DML)
├── mall_base_ddl.sql          ⭐ 表结构 (推荐先执行)
├── mall_base_dml.sql          ⭐ 数据插入 (推荐后执行)
├── equipment_renovation.sql    器材共享改造
└── equipment_handover.sql      交接会话功能
```

---

## 🎯 立即开始

### 在DataGrip中

**Step 1: 创建表**
1. 打开: `sql/postgresql/mall_base_ddl.sql`
2. 执行 ▶️
3. 等待~5秒

**Step 2: 插入数据**
1. 打开: `sql/postgresql/mall_base_dml.sql`
2. 执行 ▶️
3. 等待~1分钟

**完成！** ✅

---

## 🔍 如果DDL执行失败

如果表创建失败，请提供：
- 具体的错误消息
- 失败的表名
- 错误位置

我会立即帮您解决！

---

<p align="center">
  <b>🎊 分步执行更稳妥！</b><br>
  <br>
  <b>先执行 mall_base_ddl.sql 创建表</b><br>
  <b>再执行 mall_base_dml.sql 插入数据</b><br>
  <br>
  <i>这次一定能成功！🚀</i>
</p>

---

**推荐文件**: 
- 第1步: `sql/postgresql/mall_base_ddl.sql`
- 第2步: `sql/postgresql/mall_base_dml.sql`

