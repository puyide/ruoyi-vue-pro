# ⭐️ 康复训练器材共享平台 - 完整部署指南

> **按此顺序执行，确保成功！**

---

## 📋 正确的执行顺序

### 🔴 第0步：检查是否已有系统基础表

```sql
-- 在PostgreSQL中检查
SELECT COUNT(*) FROM information_schema.tables 
WHERE table_name LIKE 'system_%';
```

**如果结果 > 0**: 跳过第1步，直接从第2步开始  
**如果结果 = 0**: 必须先执行第1步

---

### 🔴 第1步：系统基础表（如果没有的话）

**文件**: `sql/postgresql/ruoyi-vue-pro.sql`

**包含内容**:
- system_* 系统表（字典、菜单、权限等）
- member_* 会员表
- infra_* 基础设施表

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/ruoyi-vue-pro.sql
```

**或在DataGrip中**:
- 打开 `ruoyi-vue-pro.sql`
- 点击 ▶️ 执行
- 等待完成

---

### 🟢 第2步：商城基础表（49个表）

#### 2.1 创建表结构

**文件**: `sql/postgresql/mall_base_ddl.sql` (38KB)

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_ddl.sql
```

**预期结果**: 创建49个表

#### 2.2 插入数据

**文件**: `sql/postgresql/mall_base_dml.sql` (813KB)

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_dml.sql
```

**预期结果**: 插入品牌、分类、商品等数据

---

### 🟡 第3步：器材共享改造（✅ 已修复索引冲突）

**文件**: `sql/postgresql/equipment_renovation.sql`

**包含内容**:
- product_spu扩展10个字段
- trade_order扩展10个字段
- member_credit_score表（新建）
- member_credit_log表（新建）
- equipment_reservation表（新建）
- 数据字典配置

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/equipment_renovation.sql
```

---

### 🟣 第4步：交接会话功能（可选）

**文件**: `sql/postgresql/equipment_handover.sql`

**包含内容**:
- 5个交接相关表
- 敏感词库和公共地点

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/equipment_handover.sql
```

---

## 🚀 一键部署脚本

保存为 `deploy_all.sh`:

```bash
#!/bin/bash
cd "/Users/admin/ma jia/ruoyi-vue-pro"

DB="ruoyi-vue-pro"

echo "========================================="
echo "康复训练器材共享平台 - 完整部署"
echo "========================================="

# 检查system表是否存在
SYSTEM_TABLES=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_name LIKE 'system_%'" 2>/dev/null | tr -d ' ')

if [ "$SYSTEM_TABLES" = "0" ] || [ -z "$SYSTEM_TABLES" ]; then
  echo -e "\nStep 0: 部署系统基础表..."
  psql -d $DB -f sql/postgresql/ruoyi-vue-pro.sql
  if [ $? -eq 0 ]; then
    echo "✓ 系统基础表部署完成"
  else
    echo "✗ 系统基础表部署失败"
    exit 1
  fi
else
  echo -e "\n✓ 系统基础表已存在，跳过"
fi

# Step 1: 商城表结构
echo -e "\nStep 1: 创建商城表结构..."
psql -d $DB -f sql/postgresql/mall_base_ddl.sql
if [ $? -ne 0 ]; then
  echo "✗ 失败"
  exit 1
fi
echo "✓ 商城表结构创建完成 (49个表)"

# Step 2: 商城数据
echo -e "\nStep 2: 插入商城数据..."
psql -d $DB -f sql/postgresql/mall_base_dml.sql
if [ $? -ne 0 ]; then
  echo "✗ 失败"
  exit 1
fi
echo "✓ 商城数据插入完成"

# Step 3: 器材共享改造
echo -e "\nStep 3: 应用器材共享改造..."
psql -d $DB -f sql/postgresql/equipment_renovation.sql
if [ $? -ne 0 ]; then
  echo "⚠️  器材共享改造失败"
else
  echo "✓ 器材共享改造完成"
fi

# Step 4: 交接会话功能
echo -e "\nStep 4: 部署交接会话功能..."
psql -d $DB -f sql/postgresql/equipment_handover.sql
if [ $? -ne 0 ]; then
  echo "⚠️  交接会话功能部署失败"
else
  echo "✓ 交接会话功能部署完成"
fi

# 最终验证
echo -e "\n========================================="
echo "最终验证..."
echo "========================================="

TABLE_COUNT=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public'" | tr -d ' ')
echo "✓ 总表数量: $TABLE_COUNT"

echo -e "\n各模块表数量:"
SYSTEM_COUNT=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_name LIKE 'system_%'" | tr -d ' ')
PRODUCT_COUNT=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_name LIKE 'product_%'" | tr -d ' ')
TRADE_COUNT=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_name LIKE 'trade_%'" | tr -d ' ')
CREDIT_COUNT=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_name LIKE '%credit%'" | tr -d ' ')

echo "  system_* 表: $SYSTEM_COUNT"
echo "  product_* 表: $PRODUCT_COUNT"
echo "  trade_* 表: $TRADE_COUNT"
echo "  credit相关表: $CREDIT_COUNT"

echo -e "\n========================================="
echo "🎉 部署完成！"
echo "========================================="
```

---

## ✅ 已修复的问题

### equipment_renovation.sql 更新内容

**修复前**:
```sql
CREATE INDEX idx_user_id ON member_credit_score(user_id);
CREATE INDEX idx_user_id ON member_credit_log(user_id);
CREATE INDEX idx_user_id ON equipment_reservation(user_id);
```
❌ 索引名冲突！

**修复后**:
```sql
CREATE INDEX idx_credit_score_user_id ON member_credit_score(user_id);
CREATE INDEX idx_credit_log_user_id ON member_credit_log(user_id);
CREATE INDEX idx_reservation_user_id ON equipment_reservation(user_id);
```
✅ 索引名唯一！

---

## 📁 文件清单

### 必须执行的文件（按顺序）

1. ✅ `ruoyi-vue-pro.sql` - 系统基础（如果未执行）
2. ✅ `mall_base_ddl.sql` - 商城表结构
3. ✅ `mall_base_dml.sql` - 商城数据
4. ✅ `equipment_renovation.sql` - 器材共享改造（已修复）
5. ✅ `equipment_handover.sql` - 交接会话功能

---

## 🎯 现在开始

### 在DataGrip中按顺序执行

如果系统基础表不存在：
1. 打开 `ruoyi-vue-pro.sql` → 执行 ▶️

然后：
2. 打开 `mall_base_ddl.sql` → 执行 ▶️
3. 打开 `mall_base_dml.sql` → 执行 ▶️
4. 打开 `equipment_renovation.sql` → 执行 ▶️
5. 打开 `equipment_handover.sql` → 执行 ▶️

---

<p align="center">
  <b>🎊 按顺序执行，确保成功！</b><br>
  <br>
  <b>完整顺序：</b><br>
  <b>系统基础 → 商城 → 器材共享 → 交接会话</b><br>
  <br>
  <i>所有问题已修复！🚀</i>
</p>

---

**关键修复**: 索引名冲突 ✅  
**执行顺序**: 必须先有系统基础表  
**状态**: 完全就绪

