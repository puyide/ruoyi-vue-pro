# ⚠️ 重要：正确的执行顺序

---

## 🎯 完整的执行顺序

### 第1步：系统基础表 (必须先执行！)

**文件**: `ruoyi-vue-pro.sql`

这个文件包含：
- system_* 系统表（字典、菜单、权限等）
- member_* 会员表
- infra_* 基础设施表

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/ruoyi-vue-pro.sql
```

**或在DataGrip中**:
- 打开 `ruoyi-vue-pro.sql`
- 点击 ▶️ 执行

---

### 第2步：商城基础表

#### 方式A: 分步执行（推荐）

**2.1 创建商城表结构**
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_ddl.sql
```

**2.2 插入商城数据**
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_dml.sql
```

#### 方式B: 一次性执行

```bash
psql -d ruoyi-vue-pro -f sql/postgresql/mall_base_final.sql
```

---

### 第3步：器材共享改造（已修复索引冲突）

**文件**: `equipment_renovation.sql` ✅ 已修复

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/equipment_renovation.sql
```

**包含内容**:
- product_spu表扩展（10个新字段）
- trade_order表扩展（10个新字段）
- member_credit_score表（新建）
- member_credit_log表（新建）
- equipment_reservation表（新建）
- 5类数据字典配置

---

### 第4步：交接会话功能（可选）

**文件**: `equipment_handover.sql`

**执行**:
```bash
psql -d ruoyi-vue-pro -f sql/postgresql/equipment_handover.sql
```

**包含内容**:
- equipment_handover表
- equipment_handover_message表
- equipment_feedback表
- sensitive_keyword表
- public_location表

---

## 📋 完整执行脚本

```bash
#!/bin/bash
cd "/Users/admin/ma jia/ruoyi-vue-pro"

DB="ruoyi-vue-pro"

echo "========================================="
echo "康复训练器材共享平台 - 完整部署"
echo "========================================="

# Step 1: 系统基础表（必须！）
echo -e "\nStep 1: 部署系统基础表..."
psql -d $DB -f sql/postgresql/ruoyi-vue-pro.sql
echo "✓ 系统基础表部署完成"

# Step 2: 商城表结构
echo -e "\nStep 2: 创建商城表结构..."
psql -d $DB -f sql/postgresql/mall_base_ddl.sql
echo "✓ 商城表结构创建完成"

# Step 3: 商城数据
echo -e "\nStep 3: 插入商城数据..."
psql -d $DB -f sql/postgresql/mall_base_dml.sql
echo "✓ 商城数据插入完成"

# Step 4: 器材共享改造
echo -e "\nStep 4: 应用器材共享改造..."
psql -d $DB -f sql/postgresql/equipment_renovation.sql
echo "✓ 器材共享改造完成"

# Step 5: 交接会话功能
echo -e "\nStep 5: 部署交接会话功能..."
psql -d $DB -f sql/postgresql/equipment_handover.sql
echo "✓ 交接会话功能部署完成"

# 验证
echo -e "\n验证结果..."
TABLE_COUNT=$(psql -d $DB -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public'" | tr -d ' ')
echo "✓ 总表数量: $TABLE_COUNT"

echo "========================================="
echo "🎉 部署完成！"
echo "========================================="
```

---

## ⚠️ 常见错误

### 错误1: relation "system_dict_type" does not exist

**原因**: 没有先执行系统基础SQL

**解决**: 先执行 `ruoyi-vue-pro.sql`

### 错误2: relation "idx_user_id" already exists

**原因**: 索引名冲突（已修复）

**解决**: 使用最新的 `equipment_renovation.sql` (已修复索引名)

### 错误3: product_spu/trade_order表不存在

**原因**: 没有先执行商城基础SQL

**解决**: 先执行 `mall_base_ddl.sql`

---

## 📊 依赖关系图

```
ruoyi-vue-pro.sql (系统基础)
    ↓
    ├─→ mall_base_ddl.sql (商城表结构)
    │       ↓
    │   mall_base_dml.sql (商城数据)
    ↓
equipment_renovation.sql (器材共享改造)
    ↓
equipment_handover.sql (交接会话功能)
```

---

## 🎯 在DataGrip中的执行顺序

1. **ruoyi-vue-pro.sql** ← 先执行这个！
2. **mall_base_ddl.sql**
3. **mall_base_dml.sql**
4. **equipment_renovation.sql** ← 索引名已修复
5. **equipment_handover.sql**

---

## ✅ 已修复内容

### equipment_renovation.sql 更新

- ✅ `idx_user_id` → `idx_credit_score_user_id`
- ✅ `idx_total_score` → `idx_credit_score_total_score`
- ✅ `idx_tenant_id` → `idx_credit_score_tenant_id`
- ✅ `idx_user_id` (member_credit_log) → `idx_credit_log_user_id`
- ✅ `idx_user_id` (equipment_reservation) → `idx_reservation_user_id`
- ✅ 添加了system_dict_type表不存在的提示

---

<p align="center">
  <b>⚠️ 请按正确顺序执行！</b><br>
  <br>
  <b>1️⃣ ruoyi-vue-pro.sql (系统基础)</b><br>
  <b>2️⃣ mall_base_ddl.sql + mall_base_dml.sql (商城)</b><br>
  <b>3️⃣ equipment_renovation.sql (器材共享)</b><br>
  <b>4️⃣ equipment_handover.sql (交接会话)</b><br>
</p>

---

**更新日期**: 2025-12-15  
**状态**: ✅ 索引冲突已修复  
**下一步**: 按顺序执行！

